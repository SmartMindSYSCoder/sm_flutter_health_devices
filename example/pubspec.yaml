name: sm_flutter_health_devices_example
description: "Demonstrates how to use the sm_flutter_health_devices plugin."
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  sm_flutter_health_devices:
    path: ../
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# Using remote versions for all health plugins

flutter:
  uses-material-design: true


